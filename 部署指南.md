# When 部署指南

## 1. 部署前确认

When 制品只包含 When 自身。Redis、ETCD、Kafka、HTTP 下游和观测后端都必须由部署方独立提供；不要把它们打进 When 镜像或 TGZ。

最低运行要求：

- JDK 17；
- 可访问的 Redis 与 ETCD；
- 每个运行节点唯一的 `WHEN_NODE_ID` 和 `WHEN_WORKER_ID`（0–1023）；
- 用于 HTTP、gRPC 和管理端口的网络策略；
- 若使用 FILE Sink，运行用户可写的节点本地目录。

当前实际通过的是单节点 FILE 投递闭环，详见[全流程测试报告](全流程测试报告.md)。多节点共享文件可见性、共享卷故障语义和生产 HA 演练不在该结论范围内。

## 2. 环境变量

环境变量是当前运行时的权威配置来源；`deploy/examples/application.yml` 仅是可读参考。

| 变量 | 是否必需 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `WHEN_NODE_ID` | 是 | 无 | 节点唯一标识 |
| `WHEN_WORKER_ID` | 是 | 无 | 0–1023 的唯一 worker ID |
| `WHEN_ETCD_ENDPOINTS` | 是 | 无 | 外部 ETCD 的 HTTP(S) endpoint |
| `WHEN_REDIS_HOST` | 是 | 无 | 外部 Redis 主机 |
| `WHEN_REDIS_PORT` | 否 | `6379` | Redis 端口 |
| `WHEN_HTTP_PORT` | 否 | `8080` | 业务 HTTP API |
| `WHEN_GRPC_PORT` | 否 | `9090` | 节点间 gRPC 监听端口 |
| `WHEN_NODE_HOST` | 否 | `127.0.0.1` | 对等节点可访问的本机地址 |
| `WHEN_MANAGEMENT_HOST` | 否 | `127.0.0.1` | 管理端口绑定地址 |
| `WHEN_MANAGEMENT_PORT` | 否 | `8081` | `/health`、`/ready`、`/metrics` 与管理接口 |
| `WHEN_FILE_SINK_BASE_DIR` | FILE 必需 | 无 | FILE Sink 的节点本地受控根目录 |
| `WHEN_KAFKA_BOOTSTRAP_SERVERS` | Kafka 时必需 | 空 | 外部 Kafka 地址 |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | 否 | 空 | 外部 OTLP Collector |

ETCD 与 Redis 的用户名、密码等凭证只通过进程环境、Secret 或平台密钥管理系统提供。禁止把凭证写入 Git、镜像层、ConfigMap、命令行参数或日志。

FILE Sink 的 `file.path` 必须是相对于 `WHEN_FILE_SINK_BASE_DIR` 的路径。建议根目录仅供 When 使用，并在调用方路径中加入 `message_id`，例如 `deliveries/<message_id>.bin`。

## 3. TGZ 部署

构建并校验版本化制品：

```bash
make release VERSION=1.0.0
(cd dist && shasum -a 256 -c checksums.txt)
tar -xzf dist/when-server-1.0.0.tgz
cd when-server-1.0.0
```

在运行进程前创建受控文件目录并授权给运行用户：

```bash
install -d -m 0750 /var/lib/when/sink-files
export WHEN_FILE_SINK_BASE_DIR=/var/lib/when/sink-files
export WHEN_NODE_ID=when-1
export WHEN_WORKER_ID=1
export WHEN_ETCD_ENDPOINTS=https://etcd.example.internal:2379
export WHEN_REDIS_HOST=redis.example.internal
```

使用前台或受平台管理的方式运行：

```bash
bin/when run
```

`bin/when start`、`bin/when status` 和 `bin/when stop` 仅管理当前 When 进程。`--config conf/application.example.yml` 只校验参考文件可读，不能替代环境变量。

## 4. 容器与 Kubernetes

构建 OCI 镜像：

```bash
make image VERSION=1.0.0 IMAGE_REPO=registry.example.com/when
```

Kubernetes 基础清单位于 `deploy/k8s/base/`。在应用前替换镜像、ETCD 与 Redis 示例地址，并由平台创建 `when-external-credentials` Secret。

```bash
make k8s-validate
kubectl -n when apply -k deploy/k8s/base
kubectl -n when rollout status statefulset/when --timeout=300s
```

基础 StatefulSet 使用只读根文件系统，只挂载 `/tmp` 和 `/opt/when/var` 的临时可写目录，且默认**没有** FILE Sink 根目录配置。因此启用 FILE Sink 时必须通过 Kustomize overlay 同时完成以下两项：

1. 设置 `WHEN_FILE_SINK_BASE_DIR`，例如 `/opt/when/var/file-sink`；
2. 挂载满足数据保留要求的卷到该目录，并确保 UID/GID `10001` 可写。

若使用现有 `emptyDir`，Pod 重建后文件会丢失；如交付文件需要跨 Pod 生命周期保存，应由平台提供 PVC 或受控共享存储，并先验证该存储的原子替换语义。不要把“文件在某个 Pod 内可见”误作“集群内所有节点可见”。

本地开发与验收不使用 Docker、Docker Compose 或 Testcontainers。生产镜像构建和 Kubernetes 清单验证是独立的制品流程。

## 5. 发布后检查

管理端口应只暴露给可信管理网络。检查：

```bash
curl -fsS http://<management-host>:8081/health
curl -fsS http://<management-host>:8081/ready
curl -fsS http://<management-host>:8081/metrics
```

`/ready` 只有在 Redis、ETCD、节点注册和本地角色恢复完成后才应成功。业务 HTTP 与管理端口需要分别实施网络访问控制；当前管理 API 无认证，不得直接暴露到不可信网络。

对 FILE Sink，发布后至少提交一条非敏感探测消息并确认：提交后 Redis 为 `PENDING`，到期后 Query 为 `DELIVERED`，且根目录下存在预期文件。不要在日志或巡检命令中输出真实 payload。

## 6. 升级、回滚与故障排查

- 使用不可变版本和制品 checksum；校验失败时不要部署。
- 升级或回滚时一次处理一个节点，并持续观察 `/ready`、Redis/ETCD 连通性、投递失败与延迟。
- 不能独立回滚 Redis 或 ETCD 元数据；需要版本兼容和迁移方案。
- `FILE_CONFIG` 表示 FILE 根目录或相对路径配置无效；检查 `WHEN_FILE_SINK_BASE_DIR`、绝对路径和 `..` 路径。
- `FILE_WRITE` 表示本地写入失败；检查权限、磁盘空间、文件系统健康和挂载状态，不要从日志中索取或输出 payload。
- 多条消息写同一路径会发生覆盖；这是调用方路径设计问题，不是去重机制。
