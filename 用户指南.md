# When 用户指南

## 1. When 是什么

When 接收一条带有未来交付时间和 Sink 配置的消息，将消息持久化后调度；到期时调用对应 Sink。Redis 保存消息事实，ETCD 保存节点与调度元数据，内存时间轮只保存可重建的调度索引。

When 提供至少一次投递语义，不承诺精确一次。下游或文件路径设计必须以 `message_id` 实现幂等。

当前 HTTP 业务接口前缀为 `/api/v1/messages`，完整字段以 [OpenAPI 契约](openapi/when-v1.yaml) 为准。

## 2. FILE Sink

FILE Sink 将到期消息的原始 payload 写入本地文件。它适合受控节点上的归档、导出或本地集成，不应被当作跨节点共享存储。

先在运行 When 的**每一台节点**设置受控根目录：

```bash
export WHEN_FILE_SINK_BASE_DIR=/var/lib/when/sink-files
```

调用方仅能提交相对路径，例如 `deliveries/<message_id>.bin`。绝对路径和 `../` 越界路径会被拒绝；未配置根目录时，FILE 消息会以永久配置错误结束。目录必须由运行 When 的操作系统用户可创建、可写入。

文件先写入同目录临时文件，再在文件系统支持时以原子移动替换目标文件；不支持原子移动的文件系统会回退为替换移动。不要让多个不同消息使用同一个目标路径，否则后一次成功投递会覆盖前一次内容。

## 3. 提交 FILE 延时消息

下面示例将在 10 秒后写入 `deliveries/example.bin`。`payload` 是 Base64 编码的字节；示例值仅用于演示，不要在命令历史、日志或工单中放入敏感 payload。

```bash
curl -sS -X POST http://127.0.0.1:8080/api/v1/messages \
  -H 'Content-Type: application/json' \
  -H 'X-Request-Id: example-file-submit-1' \
  --data '{
    "delay_seconds": 10,
    "sink_type": "FILE",
    "sink_config": {"file": {"path": "deliveries/example.bin"}},
    "payload": "aGVsbG8=",
    "business_tag": "local-export"
  }'
```

成功响应为 HTTP `201`，`data.message_id` 是后续查询和取消的唯一标识。响应统一使用下列信封：

```json
{
  "code": "OK",
  "message": "success",
  "request_id": "example-file-submit-1",
  "data": {
    "message_id": "<message_id>",
    "status": "PENDING",
    "created_at": 0
  }
}
```

支持两种互斥的交付时间字段：

- `delay_seconds`：1 秒至 30 天；
- `deliver_at`：未来的 Unix epoch 毫秒。

一次请求必须且只能提供其中一个。payload 最大解码长度为 64 KiB。

## 4. 查询与取消

查询消息：

```bash
curl -sS http://127.0.0.1:8080/api/v1/messages/<message_id>
```

取消尚未投递的消息：

```bash
curl -sS -X DELETE http://127.0.0.1:8080/api/v1/messages/<message_id>
```

状态流转为：

```text
PENDING -> DELIVERING -> DELIVERED
PENDING -> CANCELLED
DELIVERING -> PENDING  （可重试）
DELIVERING -> FAILED
```

取消仅对仍可取消的消息有效；已取消消息重复取消保持幂等。正常 FILE 投递成功后查询会得到 `DELIVERED`。不要把提交成功或 `PENDING` 误认为文件已经写入。

## 5. 本机快速体验

开发环境需要 JDK 17、`redis-server`、`redis-cli`、`etcd`、`etcdctl`、`curl`、`python3` 和 `perl`。Harness 会使用隔离端口和临时数据目录启动依赖。

```bash
./mvnw -q -pl when-app,when-e2e-test -am package
harness/local/start-deps.sh redis etcd
harness/local/wait-deps.sh --timeout 30 redis etcd

export WHEN_FILE_SINK_BASE_DIR="$(mktemp -d "$PWD/.loop/file-sink-demo.XXXXXX")"
export WHEN_HTTP_PORT=8080
export WHEN_MANAGEMENT_PORT=8081
harness/core-tests/start-single-node.sh
```

按上一节提交 FILE 消息后，等待其到期并检查：

```bash
test -f "$WHEN_FILE_SINK_BASE_DIR/deliveries/example.bin"
curl -sS http://127.0.0.1:8080/api/v1/messages/<message_id>
```

结束时务必停止临时进程：

```bash
harness/core-tests/stop-single-node.sh
harness/local/stop-deps.sh
```

本地开发和验收不使用 Docker Compose 或 Testcontainers。

## 6. 其他 Sink 与安全边界

HTTP 和 Kafka Sink 也在公共契约中定义；分别需要 `sink_config.http` 和 `sink_config.kafka`，字段约束见 OpenAPI 与 proto。请勿在业务请求、日志、指标或管理接口中暴露凭证、完整敏感 Sink 配置或敏感 payload。

Redis 是消息状态的权威来源；ETCD 不存 payload。日志和指标可使用 `message_id` 定位消息，但不得将 payload、URL、topic、异常文本或敏感配置作为指标标签。

遇到 `FILE_CONFIG` 时，检查 FILE 根目录是否配置、请求路径是否相对且未越界；遇到 `FILE_WRITE` 时，检查节点本地目录权限、磁盘空间和文件系统状态。错误信息不会回显文件路径或 payload。
