# When 全流程测试报告

## 结论

**本次单机 FILE Sink 全流程验收通过。** 测试使用独立的 `when-app` JVM、Harness 启动的本机 Redis 和 ETCD，以及 HTTP 业务接口；未使用 Docker、Docker Compose 或 Testcontainers。

本次实际验证的是当前运行语义：消息先持久化为 `PENDING`，到期后由 FILE Sink 写入受控本地目录，并将状态转换为 `DELIVERED`。取消消息不会创建目标文件；在到期前重启同一节点后，Redis 中的待处理消息会被重建并最终写入文件。

## 测试基线

| 项目 | 实际值 |
| --- | --- |
| 测试日期 | 2026-08-24（Asia/Shanghai） |
| 测试代码 | `481bb30 merge redis lease precision fix` |
| FILE Sink 引入提交 | `5cf69b1 merge local file sink` |
| 运行方式 | 独立 JVM Server + 本机临时 Redis/ETCD |
| 测试接口 | HTTP `POST/GET/DELETE /api/v1/messages` |
| 文件根目录 | `.loop/file-sink-full-e2e.d32AKo`（本次临时目录） |
| 原始结果 | `.loop/file-sink-full-node.JQehaA/file-sink-full-e2e.result` |
| Server 日志 | `.loop/file-sink-full-node.JQehaA/server.log` |

所有 FILE Sink 路径均相对于 `WHEN_FILE_SINK_BASE_DIR`，本次使用 `deliveries/*.bin`。测试报告不记录实际 payload 内容。

## 结果矩阵

| 场景 | 断言 | 结果 | 实际证据 |
| --- | --- | --- | --- |
| Maven 校验 | `./mvnw -q verify` 通过 | PASS | 首阶段执行日志 `reports/first-stage-e2e/20260824-161823-maven-verify.log` |
| 公共契约 | `harness/contracts/acceptance.sh` 通过 | PASS | `reports/first-stage-e2e/20260824-161823-contract-acceptance.log`，输出 `contract checks passed: lesson45` |
| 单机就绪 | gRPC health 为 `SERVING`；ETCD node/worker claim 均存在，跨 2 秒复查 | PASS | FILE E2E 执行过程中的外部 gRPC 与 `etcdctl` 断言 |
| Submit 与持久化 | HTTP 提交 FILE 消息后，Query 与 Redis `when:msg:<id>` 均确认 `PENDING` | PASS | `normal_message_id=85287688377241600`、`normal_redis_status=PENDING` |
| 到期投递 | 到期后状态为 `DELIVERED`；文件内容与提交字节完全一致；投递日志只有一条 | PASS | `normal_final_status=DELIVERED`、`normal_delivery_log_count=1` |
| 取消 | 取消后状态为 `CANCELLED`；超过原计划时间仍不存在目标文件 | PASS | `cancel_message_id=85287701547356160`、`cancel_status=CANCELLED`、`cancel_file_absent=PASS` |
| 重启恢复 | 到期前停止并以相同 node/worker/gRPC 端口重启；待处理 FILE 消息最终投递且日志一条 | PASS | `restart_message_id=85287731540824064`、`restart_final_status=DELIVERED`、`restart_delivery_log_count=1` |
| 清理 | Server、Redis、ETCD 均已停止 | PASS | `LOCAL_CLEANUP=PASS` |

执行最终断言时输出：

```text
FILE_SINK_FULL_E2E=PASS
normal_redis_status=PENDING
normal_final_status=DELIVERED
cancel_status=CANCELLED
cancel_file_absent=PASS
restart_final_status=DELIVERED
LOCAL_CLEANUP=PASS
```

## 覆盖的链路

```text
HTTP Submit(FILE)
  -> 请求校验与消息 ID
  -> Redis 持久化（PENDING）
  -> 本地时间轮
  -> 到期 DueMessageHandler
  -> FILE Sink（受控目录内原子替换写入）
  -> Redis 状态 DELIVERED
  -> 安全投递日志
```

取消路径在消息仍为 `PENDING` 时进入 `CANCELLED`，不会调用 FILE Sink。重启路径验证 Redis 是消息事实来源、时间轮是可重建索引。

## 已知边界与客观限制

- FILE Sink 是**节点本地**投递：每个 `file.path` 都相对于该节点的 `WHEN_FILE_SINK_BASE_DIR`，不是分布式共享文件系统。
- 同一目标路径被不同消息使用时，后一次成功投递会替换前一次内容；调用方应将 `message_id` 或等价唯一值纳入路径。相同消息重试使用同一目标路径。
- 当前验证是单节点 FILE 路径；未把多节点共享卷、跨节点 FILE 可见性、Kafka 外部环境、生产高可用故障演练或 Kubernetes 实际集群运行伪装成已验证结果。
- `harness/core-tests/run-single-node-flow.sh` 是第 39—45 节的旧“仅写 `event=when_due` 日志、不调用真实 Sink”验收脚本。它固定提交 HTTP Sink 并要求状态停留在 `DELIVERING`，与当前真实 Sink 投递并进入 `DELIVERED` 的运行语义不兼容；其最近一次失败证据保存在 `reports/first-stage-e2e/20260824-161823.md`。该限制不影响本报告的 FILE 实际投递断言，但应在后续阶段统一两套验收口径。

## 复现入口

构建与模块测试：

```bash
./mvnw -q verify
```

本机依赖由 Harness 管理：

```bash
harness/local/start-deps.sh redis etcd
harness/local/wait-deps.sh --timeout 30 redis etcd
# 完成验证后
harness/local/stop-deps.sh
```

FILE Sink 的配置和 HTTP 操作见根目录的[用户指南](用户指南.md)，生产部署注意事项见[部署指南](部署指南.md)。
